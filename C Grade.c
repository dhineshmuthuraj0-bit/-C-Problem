#include <stdio.h>
int main() {
    int a;
    scanf("%d",&a);
    if (a>=75 && a<=90)
        {
        printf("Grade B");
    }
    else
    {
        printf("Invalid Grade");
    }
        return 0;
    }
